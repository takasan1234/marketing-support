// import { z } from "zod";
// import { Request, Response } from "express";
// import { CreateUserCommand } from "../../application/commands/user.command.example";

// export const createUserSchema = z.object({
//   email: z.string().email(),
//   name: z.string().optional(),
// });

/**
 * Presentation層のコントローラーのサンプル実装です。
 * 開発環境のDI（依存注入）の実装例として参考にしてください。
 */
/*
export class UserController {
  constructor(private createUserCommand: CreateUserCommand) {}

  // POST /users のハンドラ
  create = async (req: Request, res: Response) => {
    try {
      // 1. バリデーション (Zod)
      const input = createUserSchema.parse(req.body);

      // 2. ユースケース実行
      const user = await this.createUserCommand.execute(input);

      // 3. レスポンス
      res.status(201).json(user);
    } catch (error) {
      if (error instanceof z.ZodError) {
        res.status(400).json({ error: error.errors });
        return;
      }
      res.status(500).json({ error: "Internal Server Error" });
    }
  };
}
*/
