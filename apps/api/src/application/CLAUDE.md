# アプリケーション層の制約
- **責務**: 業務のフロー制御（ユースケース）のみ。
- **禁止事項**: 
  - ここに具体的なDBクエリを書かないこと（Repositoryを使うこと）。
  - ここでHTTPリクエストやレスポンスを直接扱わないこと。
  - ここで複雑なビジネスロジックを書かないこと（ロジックは Entity/ValueObject へ）。
- **依存ルール**: `packages/domain` のモデルとインターフェースのみを利用すること（Response DTO の組み立ては可）。